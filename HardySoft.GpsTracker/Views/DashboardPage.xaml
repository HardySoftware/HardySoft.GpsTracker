<prism:SessionStateAwarePage
    x:Class="HardySoft.GpsTracker.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HardySoft.GpsTracker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localTemplate="using:HardySoft.GpsTracker.Template"
    xmlns:prism="using:Prism.Windows.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <localTemplate:TemplatesResourceDictionary/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="10,0,0,0">
        <TextBlock RelativePanel.AlignTopWithPanel="True" x:Name="PageTitle" Style="{StaticResource WindowTitle}">Start/stop my movement tracking</TextBlock>
        <TextBlock RelativePanel.Below="PageTitle" Style="{StaticResource CaptionTitle}">Activity</TextBlock>
        <!--<ComboBox x:Name="ActivityOptions" ItemsSource="{Binding Path=SupportedActivityTypes}">
        </ComboBox>-->
    </RelativePanel>
</prism:SessionStateAwarePage>
